using AutoMapper;
using Microsoft.EntityFrameworkCore.Metadata.Internal;
using Moq;
using PassportChecker.Services.Implementations;
using Xunit;

namespace PassportChecker.Tests;
/*
public class PassportPostgreSQLServiceTests
{
    [Fact]
    public void GetInactivePassports()
    {
        //Arrange
        var mockDb = new Mock<ApiDbContext>();
        var mockMapper = new Mock<IMapper>();
        var passportService = new PassportPostgreSQLService(mockDb.Object, mockMapper.Object);

        //Act
        var result = passportService.GetInactivePassports();
        //Assert
        Assert.True(result);
    }

    [Fact]
    public void GetChangesByDate()
    {
        //Arrange
        var mockDb = new Mock<ApiDbContext>();
        var mockMapper = new Mock<IMapper>();
        var passportService = new PassportPostgreSQLService(mockDb.Object, mockMapper.Object);


        //Act
        var result = passportService.GetChangesByDate();
        //Assert
        Assert.True(result);
    }

    [Fact]
    public void GetChangeByPassport()
    {
        //Arrange
        var mockDb = new Mock<ApiDbContext>();
        var mockMapper = new Mock<IMapper>();
        var passportService = new PassportPostgreSQLService(mockDb.Object, mockMapper.Object);

        //Act
        var result = passportService.GetChangeByPassport()
        //Assert
        Assert.True(result);
    }
}
*/